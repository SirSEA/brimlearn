import { AlertTriangle, ClipboardList, LineChart, Send, TrendingUp } from "lucide-react";
import { toast } from "sonner";
import { useState } from "react";

export function TeacherAnalytics() {
  const [subject, setSubject] = useState("All subjects");
  const [grade, setGrade] = useState("JSS1");
  const [learner, setLearner] = useState("Whole class");
  const [range, setRange] = useState("30");
  const profiles: Record<string, { trend: number[]; topics: Array<{ name: string; score: number; note: string }> }> = {
    "Whole class": { trend: [42, 48, 45, 57, 54, 63, 61, 72, 68, 78, 75, 84], topics: [{ name: "Fractions & decimals", score: 54, note: "Denominator-first answers" }, { name: "Geometry lab", score: 61, note: "Perimeter vs area" }, { name: "Multiplication", score: 73, note: "Word-problem transfer" }] },
    "Amara Okafor": { trend: [38, 42, 46, 44, 51, 56, 55, 63, 61, 70, 74, 79], topics: [{ name: "Fractions & decimals", score: 42, note: "Needs visual fraction reset" }, { name: "Multiplication", score: 68, note: "Improving word-problem transfer" }, { name: "Geometry lab", score: 76, note: "Strong shape vocabulary" }] },
    "Leo Mensah": { trend: [52, 55, 53, 58, 61, 60, 66, 64, 70, 72, 71, 77], topics: [{ name: "Multiplication", score: 48, note: "Facts secure, application shaky" }, { name: "Fractions & decimals", score: 64, note: "Occasional denominator slips" }, { name: "Geometry lab", score: 79, note: "Confident with perimeter" }] },
    "Zuri Campbell": { trend: [44, 46, 50, 49, 55, 58, 60, 59, 64, 68, 70, 73], topics: [{ name: "Geometry lab", score: 45, note: "Confusing perimeter with area" }, { name: "Fractions & decimals", score: 69, note: "Good visual reasoning" }, { name: "Multiplication", score: 77, note: "Secure core facts" }] },
  };
  const base = profiles[learner];
  const multiplier = subject === "Mathematics" ? 1 : subject === "English Studies" ? 0.94 : 0.98;
  const allScores = base.trend.map((value) => Math.round(value * multiplier));
  const visibleCount = range === "7" ? 4 : range === "30" ? 8 : allScores.length;
  const scores = allScores.slice(-visibleCount);
  const topics = base.topics.map((topic) => ({ ...topic, score: Math.round(topic.score * multiplier) }));
  const dates = scores.map((_, index) => new Date(Date.now() - (scores.length - index - 1) * 7 * 86400000).toLocaleDateString(undefined, { month: "short", day: "numeric" }));
  const average = Math.round(scores.reduce((sum, value) => sum + value, 0) / scores.length);
  const reportRows = scores.map((score, index) => ({ date: dates[index], score, topic: topics[index % topics.length]?.name || "Overall confidence" }));
  const shareWithParent = () => { if (learner === "Whole class") { toast("Select an individual learner before sharing with a parent."); return; } toast.success(`Filtered ${subject === "All subjects" ? "performance" : subject} insights sent to ${learner}'s parent.`); };
  const exportCsv = () => { const csv = ["Date,Score,Topic", ...reportRows.map((row) => `${row.date},${row.score}%,"${row.topic}"`)].join("\\n"); const url = URL.createObjectURL(new Blob([csv], { type: "text/csv;charset=utf-8" })); const link = document.createElement("a"); link.href = url; link.download = `brimlearn-${learner.replaceAll(" ", "-").toLowerCase()}-${range}days.csv`; link.click(); URL.revokeObjectURL(url); toast.success("Filtered CSV report downloaded."); };
  const exportPdf = () => { toast("Opening the filtered report print dialog — choose Save as PDF."); window.print(); };
  return <section className="mt-7 rounded-[27px] border border-[#e3e8df] bg-white p-6 shadow-[0_12px_30px_rgba(26,53,40,.05)] sm:p-7"><div className="flex flex-col gap-4 xl:flex-row xl:items-start xl:justify-between"><div><div className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#8aa096]">Teacher analytics</div><h2 className="mt-1 font-display text-2xl font-semibold tracking-[-0.05em] text-[#183c31]">Class-wide performance trends.</h2><p className="mt-2 text-sm text-[#7d958b]">{learner === "Whole class" ? `JSS1 · filtered reporting view` : `${learner} · ${grade} · filtered learner view`}</p></div><div className="grid h-10 w-10 place-items-center rounded-xl bg-[#e5f5ed] text-[#32805a]"><LineChart size={18} /></div></div><div className="mt-6 grid gap-3 md:grid-cols-4"><label className="text-xs font-semibold text-[#527064]">Subject<select value={subject} onChange={(event) => setSubject(event.target.value)} className="mt-2 w-full rounded-xl border border-[#e1e8df] bg-white px-3 py-2.5 text-xs font-semibold text-[#25483c]"><option>All subjects</option><option>Mathematics</option><option>English Studies</option></select></label><label className="text-xs font-semibold text-[#527064]">Grade level<select value={grade} onChange={(event) => setGrade(event.target.value)} className="mt-2 w-full rounded-xl border border-[#e1e8df] bg-white px-3 py-2.5 text-xs font-semibold text-[#25483c]">{["JSS1", "JSS2", "JSS3", "SS1", "SS2", "SS3"].map((item) => <option key={item}>{item}</option>)}</select></label><label className="text-xs font-semibold text-[#527064]">Individual learner<select value={learner} onChange={(event) => setLearner(event.target.value)} className="mt-2 w-full rounded-xl border border-[#e1e8df] bg-white px-3 py-2.5 text-xs font-semibold text-[#25483c]"><option>Whole class</option><option>Amara Okafor</option><option>Leo Mensah</option><option>Zuri Campbell</option></select></label><label className="text-xs font-semibold text-[#527064]">Date range<select value={range} onChange={(event) => setRange(event.target.value)} className="mt-2 w-full rounded-xl border border-[#e1e8df] bg-white px-3 py-2.5 text-xs font-semibold text-[#25483c]"><option value="7">Last 7 days</option><option value="30">Last 30 days</option><option value="90">Last 90 days</option></select></label></div><div className="mt-6 grid gap-5 xl:grid-cols-[1.1fr_.9fr]"><div><div className="flex items-center justify-between"><div className="text-xs font-semibold text-[#25483c]">Confidence over the last {range} days</div><div className="text-xs font-semibold text-[#34775e]">{average}% average</div></div><div className="mt-3 h-36 rounded-2xl bg-[#f6f8f3] p-4"><div className="flex h-full items-end gap-2">{scores.map((score, index) => <div key={`${dates[index]}-${index}`} title={`${dates[index]} · ${score}% · ${reportRows[index]?.topic}`} className="group relative flex flex-1 items-end rounded-t-lg bg-[#75a889] transition-all duration-200 hover:bg-[#34775e]" style={{ height: `${score}%`, opacity: index < Math.max(1, scores.length - 8) ? 0.45 : 0.9 }}><span className="pointer-events-none absolute -top-12 left-1/2 z-10 hidden -translate-x-1/2 whitespace-nowrap rounded-lg bg-[#173f31] px-2 py-1 text-[10px] font-semibold text-white shadow-lg group-hover:block">{dates[index]} · {score}%<br />{reportRows[index]?.topic}</span></div>)}</div></div><div className="mt-4 flex items-center gap-2 text-xs text-[#527064]"><TrendingUp size={14} className="text-[#3b926f]" /><span className="font-semibold text-[#34775e]">+{Math.max(6, scores[scores.length - 1] - scores[0])} points</span> for this filtered view</div></div><div className="rounded-2xl bg-[#fffaf6] p-5"><div className="flex items-center gap-2 text-[11px] font-semibold uppercase tracking-[0.18em] text-[#a25142]"><AlertTriangle size={14} /> Common struggle topics</div><div className="mt-4 space-y-4">{topics.map((topic) => <div key={topic.name}><div className="flex items-center justify-between text-xs"><span className="font-semibold text-[#25483c]">{topic.name}</span><span className="text-[#a25142]">{topic.score}% confident</span></div><div className="mt-2 h-2 rounded-full bg-[#f0e3dc]"><div className="h-full rounded-full bg-[#ff9a87] transition-all duration-200" style={{ width: `${topic.score}%` }} /></div><div className="mt-1 text-[11px] text-[#8aa096]">Most common: {topic.note}</div></div>)}</div></div></div><div className="mt-5 flex flex-col gap-3 border-t border-[#edf1e9] pt-5 xl:flex-row xl:items-center xl:justify-between"><div className="flex flex-wrap gap-2"><button onClick={shareWithParent} className="rounded-full bg-[#fff1d7] px-4 py-2.5 text-xs font-semibold text-[#8d6723] hover:bg-[#ffe8b8]"><Send className="mr-1.5 inline" size={13} /> Share with Parent</button><button onClick={exportCsv} className="rounded-full bg-[#e5f5ed] px-4 py-2.5 text-xs font-semibold text-[#34775e] hover:bg-[#d5eddf]"><ClipboardList className="mr-1.5 inline" size={13} /> Export CSV</button><button onClick={exportPdf} className="rounded-full bg-[#edf2ff] px-4 py-2.5 text-xs font-semibold text-[#4f6da8] hover:bg-[#e0e8ff]"><ClipboardList className="mr-1.5 inline" size={13} /> Export PDF</button></div><button onClick={() => toast.success(`A focused ${subject === "All subjects" ? "cross-subject" : subject} intervention pack is ready to plan.`)} className="rounded-full bg-[#173f31] px-4 py-3 text-xs font-semibold text-white hover:bg-[#286b51]">Plan filtered intervention</button></div></section>;
}
