import { AlertTriangle, BarChart3, BookOpen, Check, Send, Sparkles, Target, TrendingUp } from "lucide-react";
import { toast } from "sonner";
import { useState } from "react";
import { ParentPortal } from "@/features/parent/ParentPortal";
import { TeacherAnalytics } from "@/features/tutor/TeacherAnalytics";
import { Mode } from "@/components/layout/shell";

export function AdultDashboard({ mode }: { mode: Exclude<Mode, "learner"> }) {
  const [alertState, setAlertState] = useState<Record<string, "open" | "assigned" | "dismissed">>({});
  const isFamily = mode === "family";
  const isClassroom = mode === "classroom";
  const title = isFamily ? "See learning, not just scores." : isClassroom ? "One pulse for every learner." : "Make school-wide growth visible.";
  const subtitle = isFamily ? "A calm, useful view of Amira’s maths rhythm — with the next best action always close by." : isClassroom ? "Spot the misconceptions hiding behind averages, then respond before they become gaps." : "From cohort health to intervention lift, BrimLearn gives leaders a shared operating picture.";
  const classroomAlerts = [
    { id: "amara", student: "Amara Okafor", initials: "AO", issue: "Fractions & decimals", signal: "3 low-confidence attempts", detail: "Repeatedly choosing denominator-first answers", lesson: "Fractions: build the whole", lessonMeta: "8 min · visual reset", tone: "bg-[#fff0ec] text-[#a25142]", priority: "High priority" },
    { id: "leo", student: "Leo Mensah", initials: "LM", issue: "Multiplication", signal: "12% accuracy drop", detail: "Fluent with facts, stuck on word problems", lesson: "Multiplication in the market", lessonMeta: "10 min · real-world", tone: "bg-[#fff5dd] text-[#9a6a21]", priority: "Needs a nudge" },
    { id: "zuri", student: "Zuri Campbell", initials: "ZC", issue: "Geometry lab", signal: "2 missed checkpoints", detail: "Confusing perimeter with area", lesson: "Perimeter vs area: spot the difference", lessonMeta: "7 min · worked examples", tone: "bg-[#eaf2ff] text-[#3975aa]", priority: "Watch list" },
  ];
  const openAlerts = classroomAlerts.filter((alert) => alertState[alert.id] !== "dismissed").length;
  const handleAlertAction = (id: string, action: "assigned" | "dismissed", lesson: string) => {
    setAlertState((current) => ({ ...current, [id]: action }));
    toast.success(action === "assigned" ? `${lesson} assigned to the learner.` : "Alert snoozed for this demo.");
  };
  return <>
    <section className="rounded-[27px] bg-[#173f31] p-7 text-white shadow-[0_18px_40px_rgba(18,61,48,.14)] sm:p-9"><div className="max-w-3xl"><div className="mb-4 inline-flex items-center gap-2 rounded-full bg-white/10 px-3 py-1.5 text-[11px] font-semibold text-[#d8f36a]"><BarChart3 size={13} /> {isFamily ? "Family pulse" : isClassroom ? "Classroom pulse" : "School pulse"}</div><h1 className="font-display text-[34px] font-semibold leading-[1.04] tracking-[-0.06em] sm:text-[44px]">{title}</h1><p className="mt-4 max-w-2xl text-sm leading-6 text-[#c4ded0]">{subtitle}</p></div><div className="mt-8 grid gap-3 sm:grid-cols-3">{(isFamily ? [["4", "practice days"], ["82%", "concept confidence"], ["12m", "today’s target"]] : isClassroom ? [["28", "learners on path"], ["6", "need a nudge"], ["91%", "assignment complete"]] : [["184", "learners active"], ["12", "classrooms live"], ["+14%", "term growth"]]).map(([value, label]) => <div key={label} className="rounded-2xl border border-white/10 bg-white/[0.07] p-4"><div className="font-display text-3xl font-semibold tracking-[-0.06em] text-[#d8f36a]">{value}</div><div className="mt-1 text-xs text-[#c4ded0]">{label}</div></div>)}</div></section>
    {isFamily && <ParentPortal />}
    {isClassroom && <TeacherAnalytics />}
    {isClassroom && <section className="mt-7 rounded-[27px] border border-[#f0d6c9] bg-[#fffaf6] p-6 shadow-[0_12px_30px_rgba(164,91,67,.05)] sm:p-7"><div className="flex flex-col gap-4 sm:flex-row sm:items-start sm:justify-between"><div><div className="flex items-center gap-2 text-[11px] font-semibold uppercase tracking-[0.18em] text-[#a25142]"><AlertTriangle size={14} /> Intervention center</div><h2 className="mt-2 font-display text-2xl font-semibold tracking-[-0.05em] text-[#183c31]">Catch the wobble before it becomes a gap.</h2><p className="mt-2 max-w-2xl text-sm leading-6 text-[#7c827b]">BrimLearn flags patterns — not labels — and pairs each alert with a focused lesson you can assign in one click.</p></div><div className="flex shrink-0 items-center gap-2 rounded-full bg-[#fff0ec] px-3 py-2 text-xs font-semibold text-[#a25142]"><span className="h-2 w-2 rounded-full bg-[#ff7b66]" /> {openAlerts} active alerts</div></div><div className="mt-6 space-y-3">{classroomAlerts.map((alert) => { const state = alertState[alert.id] || "open"; return <div key={alert.id} className={`rounded-2xl border p-4 transition ${state === "dismissed" ? "border-[#e3e8df] bg-[#f6f8f3] opacity-60" : "border-[#eee4dc] bg-white"}`}><div className="flex flex-col gap-4 xl:flex-row xl:items-center"><div className="flex min-w-0 flex-1 items-start gap-3"><div className="grid h-10 w-10 shrink-0 place-items-center rounded-full bg-[#d8f36a] text-xs font-bold text-[#31583f]">{alert.initials}</div><div className="min-w-0"><div className="flex flex-wrap items-center gap-2"><span className="font-semibold text-[#25483c]">{alert.student}</span><span className={`rounded-full px-2 py-1 text-[10px] font-semibold ${alert.tone}`}>{alert.priority}</span></div><div className="mt-1 text-xs font-semibold text-[#527064]">{alert.issue} · {alert.signal}</div><div className="mt-1 text-xs text-[#8aa096]">{alert.detail}</div></div></div><div className="flex min-w-0 items-center gap-3 rounded-2xl bg-[#f6f8f3] p-3 xl:w-[330px]"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-white text-[#34775e] shadow-sm"><BookOpen size={15} /></div><div className="min-w-0 flex-1"><div className="truncate text-xs font-semibold text-[#25483c]">Suggested: {alert.lesson}</div><div className="mt-1 text-[11px] text-[#8aa096]">{alert.lessonMeta}</div></div></div><div className="flex items-center gap-2 xl:w-[190px] xl:justify-end">{state === "assigned" ? <div className="flex items-center gap-2 rounded-full bg-[#e1f3e8] px-3 py-2 text-xs font-semibold text-[#32805a]"><Check size={13} /> Assigned</div> : state === "dismissed" ? <div className="text-xs font-semibold text-[#8aa096]">Snoozed</div> : <><button onClick={() => handleAlertAction(alert.id, "dismissed", alert.lesson)} className="rounded-full px-3 py-2 text-xs font-semibold text-[#8aa096] hover:bg-[#f4f7ef]">Snooze</button><button onClick={() => handleAlertAction(alert.id, "assigned", alert.lesson)} className="rounded-full bg-[#173f31] px-3 py-2 text-xs font-semibold text-white hover:bg-[#286b51]">Assign lesson</button></>}</div></div></div>; })}</div><div className="mt-5 flex items-center gap-2 text-xs text-[#8aa096]"><Sparkles size={14} className="text-[#c58e3d]" /> Alerts refresh after each checkpoint and practice session.</div></section>}
    <section className="mt-7 grid gap-5 xl:grid-cols-[1fr_1fr]">
      <div className="rounded-[27px] border border-[#e3e8df] bg-white p-6 sm:p-7"><div className="flex items-start justify-between"><div><div className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#8aa096]">Next best actions</div><h2 className="mt-1 font-display text-2xl font-semibold tracking-[-0.05em] text-[#183c31]">Do less admin. See more learning.</h2></div><div className="grid h-10 w-10 place-items-center rounded-xl bg-[#e5f5ed] text-[#32805a]"><Target size={18} /></div></div><div className="mt-6 space-y-3">{(isFamily ? [["Ask Amira to explain 3/4", "Builds reasoning confidence", "Ask"], ["Review the 10s lesson together", "A 5 minute co-pilot moment", "Open"], ["Send a teacher note", "Keep home and school in sync", "Message"]] : isClassroom ? [["Group 6 learners for a 10s reset", "Shared misconception detected", "Plan"], ["Send Tuesday’s practice pack", "12 minutes · auto-marked", "Send"], ["Review Amira’s explanation", "Reasoning is trending up", "View"]] : [["Open Year 7 intervention view", "3 cohorts trending below target", "Open"], ["Share this week’s pulse", "For leadership stand-up", "Share"], ["Review curriculum coverage", "8 concepts need a closer look", "Review"]]).map(([label, note, action]) => <div key={label} className="flex items-center gap-3 rounded-2xl bg-[#f6f8f3] p-3.5"><div className="grid h-9 w-9 shrink-0 place-items-center rounded-xl bg-white text-[#34775e] shadow-sm"><Check size={16} /></div><div className="min-w-0 flex-1"><div className="truncate text-sm font-semibold text-[#25483c]">{label}</div><div className="mt-1 text-xs text-[#8aa096]">{note}</div></div><button onClick={() => toast(`${action} action queued for this demo.`)} className="rounded-full bg-white px-3 py-2 text-xs font-semibold text-[#34775e] shadow-sm hover:bg-[#e8f2e9]">{action}</button></div>)}</div></div>
      <div className="rounded-[27px] border border-[#e3e8df] bg-white p-6 sm:p-7"><div className="flex items-start justify-between"><div><div className="text-[11px] font-semibold uppercase tracking-[0.18em] text-[#8aa096]">Progress over time</div><h2 className="mt-1 font-display text-2xl font-semibold tracking-[-0.05em] text-[#183c31]">Confidence is moving up.</h2></div><div className="rounded-full bg-[#e5f5ed] px-3 py-1.5 text-xs font-semibold text-[#34775e]">Last 30 days</div></div><div className="mt-6 h-[170px] rounded-2xl bg-[#f6f8f3] p-4"><div className="flex h-full items-end gap-2">{[36, 44, 41, 56, 53, 64, 62, 78, 72, 86, 82, 94].map((height, index) => <div key={index} className="relative flex-1 rounded-t-lg bg-[#d8f36a]" style={{ height: `${height}%`, opacity: index < 4 ? 0.45 : 0.9 }}><div className="absolute -top-5 left-1/2 hidden -translate-x-1/2 text-[9px] text-[#7b9488] group-hover:block">{height}</div></div>)}</div></div><div className="mt-4 flex items-center gap-2 text-xs text-[#527064]"><TrendingUp size={14} className="text-[#3b926f]" /> <span className="font-semibold text-[#34775e]">+12 points</span> average confidence since baseline</div></div>
    </section>
  </>;
}
